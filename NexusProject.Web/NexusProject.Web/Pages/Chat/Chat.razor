@page "/chat"
@inject IChatService ChatService


<div class="chat-app">
    <ContactList ContactSelected="OnContactSelected" />

    @if (SelectedContact != null)
    {
        <ChatWindow Contact="SelectedContact" Messages="messages" />
    }
</div>

@code {
    private User SelectedContact;
    private List<Message> messages = new List<Message>();

    private async Task OnContactSelected(User contact)
    {
        SelectedContact = contact;
        messages = await ChatService.GetConversationAsync("1", contact.Document); 
    }
}